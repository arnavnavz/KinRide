# Prisma 7: Node 20.19+, 22.12+, or 24+. Pin 20.19 so Nixpacks does not pick 22.11.x.
# Railway sets production npm config; without --include=dev, `next build` breaks (TS, Tailwind, etc.).
[variables]
NODE_VERSION = "20.19"

[phases.install]
cmds = ["npm ci --include=dev"]
